import { EffectValuePipe } from './effect-value.pipe';

describe('EffectValuePipe', () => {
  it('create an instance', () => {
    const pipe = new EffectValuePipe();
    expect(pipe).toBeTruthy();
  });
});
